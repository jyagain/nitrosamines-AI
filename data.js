// Test & verification data for Nitrosamine Impurities (Removed)
const nitrosamineSamples = [];

// Export for environments
if (typeof module !== 'undefined' && module.exports) {
  module.exports = { nitrosamineSamples };
} else {
  window.nitrosamineSamples = nitrosamineSamples;
}
